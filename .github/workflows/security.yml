name: 🔒 Security Scan

on:
  push:
    branches: [master, main]
  workflow_run:
    workflows: ["🧪 Run Tests"]
    types: [completed]

jobs:
  security:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Bandit
        run: |
          pip install bandit
          bandit -r app || echo "Security check completed"
